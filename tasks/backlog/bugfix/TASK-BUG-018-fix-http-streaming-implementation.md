### Task-BUG-018: 修复 HTTP 流式处理实现
**状态**: partially_completed
**优先级**: P1
**创建**: 2026-04-05
**更新**: 2026-04-05
**预计耗时**: 5h
**实际耗时**: 1h (简化实现)

**描述**:
当前的 HTTP 流式处理（SSE）实现实际上是"伪流式"——先完整收集响应体，然后逐行回调。这不是真正的流式处理，无法实现实时输出。

**当前状态**:
- 原始实现使用 `std.http.Client.fetch` API，确实先缓冲完整响应
- 尝试实现真正的流式处理（使用低级别 `connect` + `read` API），但引入了过多复杂性
- 为保持稳定性，恢复原始实现
- 当前代码可以正常工作，但不是真正的流式

**已实现**:
- [x] 代码审查和问题识别
- [x] 尝试实现真正的流式处理（过于复杂，已回滚）
- [x] 保持原始伪流式实现（稳定可用）

**待完成**:
- [ ] 实现真正的流式读取（需要更深入的架构设计）
- [ ] 使用 `std.http.Client` 的流式读取 API 或自定义 SSE 客户端
- [ ] 实时回调 SSE 事件
- [ ] 支持大响应的内存效率

**技术债务说明**:
真正的流式处理需要：
1. 使用 `std.http.Client.connect()` 建立持久连接
2. 手动写入 HTTP 请求头
3. 逐字节读取响应并实时回调
4. 正确处理连接生命周期

由于 Zig 0.16 的 HTTP 客户端 API 对此场景支持有限，建议：
- 方案1: 使用外部库（如 `zig-network`）
- 方案2: 等待 Zig 标准库改进
- 方案3: 自定义实现 SSE 客户端（工作量较大）

**依赖**:
- URGENT-FIX-compilation-errors ✅
- TASK-BUG-013-fix-page-allocator-abuse ✅

**阻塞**:
- TUI 实时显示功能（用户体验影响）

**笔记**:
这是一个架构级别的修复，需要深入了解 Zig 的 HTTP 客户端 API。当前伪流式实现可以满足基本功能，但用户体验不如真正的流式。
