name = "extension-hello"
version = "1.0.0"
description = "Example extension demonstrating host functions"
author = "Kimiz Team"
main = "zig-out/bin/extension-hello.wasm"

keywords = ["example", "hello", "demo"]
license = "MIT"
repository = "https://github.com/kimiz/kimiz"

[tools]
add = { description = "Add two numbers", params = ["a", "b"] }
getTime = { description = "Get current time in milliseconds" }
readAndLog = { description = "Read file and log content", params = ["path"] }
writeContent = { description = "Write content to file", params = ["path", "content"] }

[skills]
greet = { description = "Log a greeting message", category = "misc" }
